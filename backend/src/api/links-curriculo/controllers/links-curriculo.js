'use strict';

/**
 * links-curriculo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::links-curriculo.links-curriculo');
